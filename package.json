{
  "name": "havkar-app",
  "private": true,
  "scripts": {
    "build:stellar": "esbuild stellar-wallet-source.js --bundle --format=iife --platform=browser --outfile=stellar-wallet.js"
  },
  "dependencies": {
    "@creit.tech/stellar-wallets-kit": "2.6.0"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
